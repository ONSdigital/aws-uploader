jobs:
  - name: hello-world-job
    plan:
      - task: print-hello-world
        config:
          platform: ubuntu-latest
          image_resource:
            type: registry-image
            source:
              reporsitory: busybox
        run:
          path: echo
          args: ["Hello, World!"]