---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: 18

inputs:
  - name: repo-git

run:
  path: sh
  args:
    - -exc
    - |
      cd repo-git
      npm install
      npm run test:unit
