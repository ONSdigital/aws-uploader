aws:
  s3_bucket: tf-state-uploaderdyme
  tfstate: terraform/aws-uploader.tfstate
  tflocks: tf-state-uploader-dyme-locks
  access_key: ((ons-ingest-preprod_access_key))
  secret_key: ((ons-ingest-preprod_secret_key))
  backend_role: arn:aws:iam::307946647091:role/aws_shared_concourse
git:
  repo: aws-uploader
  branch: preprod
terraform:
  tfvars: env/preprod.tfvars
concourse:
  vars: preprod-vars.yml
