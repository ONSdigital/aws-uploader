aws:
  s3_bucket: my_bucket_name
  tfstate: my_tfstatefile.tfstate
  tflocks: locks-table
  access_key: ((secret_key_from_secrets))
  secret_key: ((secret_key_from_secrets))
git:
  repo: aws-Uploader
  branch:
    repo-git: new_pipeline
    repo-git-pr: new_pipeline
terraform:
  tfvars: env/env.tfvars
