aws:
  s3_bucket: tf-state-uploader-9w3xx5nt
  tfstate: terraform/aws-uploader.tfstate
  tflocks: tf-state-uploader-9w3xx5nt
  access_key: ((ingest_dev_aws_access_key))
  secret_key: ((ingest_dev_aws_secret_key))
git:
  repo: aws-Uploader
    branch:
      repo-git: new_pipeline
      repo-git-pr: new_pipeline
terraform:
  tfvars: env/dev.tfvars
